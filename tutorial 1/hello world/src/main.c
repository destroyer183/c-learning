#include <stdio.h>

// this is the main method that will get executed when you run the code.
// whenever the computer executes our C program, it is going to look inside this main method, 
// mainly at the code inside of the open and closed curly brackets, and it is going to execute that code
int main(int argc, char const *argv[])
{
    printf("Hello World!\n"); // every line of C must conclude with a semicolon (;)
    return 0;
}
